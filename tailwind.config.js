const tailwindConfig = {
  theme: {
    extend: {
      colors: {
        black75: "rgb(0 0 0 / 75%)", // <-- Исправленный вариант
      },
    },
  },
  plugins: [],
};

export default tailwindConfig;
